import React, { FC } from 'react';
import Container from '../../Container/Container';

const Settings: FC = () => {
  return (
    <div>
      <Container>Здесь должны быть настройки</Container>
    </div>
  );
};

export default Settings;
